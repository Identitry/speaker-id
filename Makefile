.PHONY: dev run test lint docker-build docker-run\n\ndev:\n\tpoetry install\nrun:\n\tpoetry run uvicorn app.main:APP --host 0.0.0.0 --port ${PORT:-8080}\ntest:\n\tpoetry run pytest -q\nlint:\n\tpoetry run ruff check app tests\n\ndocker-build:\n\tdocker build -f docker/Dockerfile -t ghcr.io/${USER}/speaker-id:local .\ndocker-run:\n\tdocker run --rm -p 8080:8080 ghcr.io/${USER}/speaker-id:local\n